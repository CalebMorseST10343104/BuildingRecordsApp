@model BuildingRecordsApp.Models.FormViewModels.AgentFormViewModel

@* 
new style:
form: <form method="post" class="needs-validation" novalidate>
input blocks: <div class="mb-3">
label: <label asp-for="[model.field]" class="form-label"></label>
input: <input asp-for="[model.field]" class="form-control" />
validation: <span asp-validation-for="[model.field]" class="text-danger"></span>
when using a select element, use:
select: <select asp-for="[model.field]" asp-items="[model.selectList]" class="form-select">
 *@

<div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
<input asp-for="AgentId" type="hidden" />

<div class="mb-3">
    <label asp-for="PersonId" class="form-label"></label>
    <select asp-for="PersonId" asp-items="Model.PersonSelectList" class="form-select">
        <option value="">Select person</option>
    </select>
    <span asp-validation-for="PersonId" class="text-danger"></span>
</div>

<div class="mb-4">
    <label asp-for="AgentCompanyId" class="form-label"></label>
    <select asp-for="AgentCompanyId" asp-items="Model.AgentCompanySelectList" class="form-select">
        <option value="">Select agent company</option>
    </select>
    <span asp-validation-for="AgentCompanyId" class="text-danger"></span>
</div>
