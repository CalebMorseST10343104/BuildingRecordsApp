@page
@model BuildingRecordsApp.Pages.StoreRooms.EditModel
@{
    ViewData["Title"] = "Edit Store Room";
}

<h1>Edit Store Room</h1>

<form method="post">
    <input type="hidden" name="StoreRoomId" value="@Model.ViewModel.StoreRoomId" />
    
    @await Html.PartialAsync("_Form", Model.ViewModel)

    <button type="submit" class="btn btn-primary">Edit</button>
    <a asp-page="./Index" class="btn btn-secondary">Back to List</a>
</form>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
