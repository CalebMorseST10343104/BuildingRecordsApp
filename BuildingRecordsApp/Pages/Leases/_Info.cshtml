@model BuildingRecordsApp.Models.DisplayViewModels.LeaseDisplayViewModel

@switch (Model.DisplayLayout)
{
    case Enums.DisplayLayout.Table:
        {
            var headers = Model.Entries.FirstOrDefault()?.GetHeaders(Model.DisplayMode) ?? new List<string> { "No Data Available" };
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        @foreach (var header in headers)
                        {
                            <th>@header</th>
                        }
                    </tr>
                </thead>
                <tbody>
                    @foreach (var lease in Model.Entries)
                    {
                        <tr>
                            @foreach (var value in lease.GetValues(Model.DisplayMode))
                            {
                                <td>@value.Value</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        }
        break;
    case Enums.DisplayLayout.List:
        {
            foreach (var entry in Model.Entries)
            {
                // This type of entry has a clear identifying header, so we can use it as a title for the entry.
                bool hasHeader = entry.BuildingName != null && entry.UnitNumber != null;
                var headerText = hasHeader ? $"{entry.BuildingName} - {entry.UnitNumber}" : "Lease Details";
                <div class="mb-4 border rounded p-3">
                    <h4>@headerText</h4>
                    <dl>
                        @foreach (var value in entry.GetValues(Model.DisplayMode))
                        {
                            @if (value.Key != "BuildingName" && value.Key != "UnitNumber")
                            {
                                <dt>@value.Key</dt>
                                <dd>@value.Value</dd>
                            }
                        }
                    </dl>
                </div>
                <br />
            }
        }
    break;
}