@page
@model BuildingRecordsApp.Pages.CompanyTrusts.IndexModel
@{
    ViewData["Title"] = "Companies/Trusts";
}

<h1>Company/Trust Index</h1>

<p>
    <a asp-page="./Create" class="btn btn-primary">Create New Company Trust</a>
</p>

<div>
    @{
        var BasePath = "BuildingRecordsApp/Pages/CompanyTrusts";
        var CompType = typeof(BuildingRecordsApp.Components.InfoDisplay<BuildingRecordsApp.Models.ItemViewModels.CompanyTrustItemViewEntry>);
        <component type="CompType"
            render-mode="Static"
            param-Model="Model.ViewModel"
            param-BasePath="BasePath" />
    }
</div>