@model BuildingRecordsApp.Models.DisplayViewModels.OwnerDisplayViewModel

@switch (Model.DisplayLayout)
{
    case Enums.DisplayLayout.Table:
        {
            var headers = Model.Entries.FirstOrDefault()?.GetHeaders(Model.DisplayMode) ?? new List<string> { "No Data Available" };
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        @foreach (var header in headers)
                        {
                            <th>@header</th>
                        }
                    </tr>
                </thead>
                <tbody>
                    @foreach (var owner in Model.Entries)
                    {
                        <tr>
                            @foreach (var value in owner.GetValues(Model.DisplayMode))
                            {
                                <td>@value.Value</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        }
        break;
    case Enums.DisplayLayout.List:
        {
            foreach (var entry in Model.Entries)
            {
                // This type of entry does not have a clear identifying header, so we can use a generic title.
                var headerText = "Owner Details";
                <div class="mb-4 border rounded p-3">
                    <h4>@headerText</h4>
                    <dl>
                        @foreach (var value in entry.GetValues(Model.DisplayMode))
                        {
                            <dt>@value.Key</dt>
                            <dd>@value.Value</dd>
                        }
                    </dl>
                </div>
                <br />
            }
        }
    break;
}