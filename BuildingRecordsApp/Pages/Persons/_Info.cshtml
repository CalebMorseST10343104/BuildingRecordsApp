@model BuildingRecordsApp.Models.DisplayViewModels.PersonDisplayViewModel

@switch (Model.DisplayLayout)
{
    case Enums.DisplayLayout.Table:
        {
            var headers = Model.People.FirstOrDefault()?.GetHeaders(Model.DisplayMode) ?? new List<string> { "No Data Available" };
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        @foreach (var header in headers)
                        {
                            <th>@header</th>
                        }
                    </tr>
                </thead>
                <tbody>
                    @foreach (var person in Model.People)
                    {
                        <tr>
                            @foreach (var value in person.GetValues(Model.DisplayMode))
                            {
                                <td>@value.Value</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        }
        break;
    case Enums.DisplayLayout.List:
        {
            foreach (var entry in Model.People)
            {
                // This type of entry has a clear identifying header, so we can use it as a title for the entry.
                bool hasHeader = entry.FirstName != null || entry.LastName != null;
                var headerText = hasHeader ? $"{entry.FirstName} {entry.LastName}" : "Person Details";
                <div class="mb-4 border rounded p-3">
                    <h4>@headerText</h4>
                    <dl>
                        @foreach (var value in entry.GetValues(Model.DisplayMode))
                        {
                            @if (value.Key != "FirstName" && value.Key != "LastName")
                            {
                                <dt>@value.Key</dt>
                                <dd>@value.Value</dd>
                            }
                        }
                    </dl>
                </div>
                <br />
            }
        }
    break;
}